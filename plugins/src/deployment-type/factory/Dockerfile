FROM ignite/shared:latest
# Inherited from ignite/shared, declared so the non-root guarantee survives a
# base-image change (and diff-aware scanners can see it).
USER plugin
CMD ["sleep", "infinity"]
